class TaskMember {
    taskId: number;
    memberId: number;

    constructor(taskId: number, memberId: number) {
        this.taskId = taskId;
        this.memberId = memberId;
    }
}

export default TaskMember;